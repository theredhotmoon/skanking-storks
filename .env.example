# URL of the quote-service backend (no trailing slash).
# Docker default: http://localhost:8080
# Empty = use Vite proxy (recommended for dev). Set to full URL only for production builds.
VITE_API_URL=
